import styled from 'styled-components';

export const Button = styled.button`
  border: none;
  cursor: pointer;
  font-weight: bold;
  font-size: 1.5rem;
  outline: none;

  &:hover {
    color: red;
  }
`;
